Var
   valor,soma,l,ct:inteiro

Inicio

   escreval("Somatoria de números")
   escreval("Escreva a quantia de somas: ")
   leia(l)
   soma <- 0
   para ct <- 1 ate l faca
      escreval("Digite um valor: ")
      leia (valor)
      soma <- soma + valor
      escreval(soma)
   fimpara


Fimalgoritmo
